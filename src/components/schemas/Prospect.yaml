type: object
properties:
  id:
    type: string
    format: uuid
  userId:
    type: integer
    format: int64
  organizationId:
    type: integer
    format: int64
  propertyId:
    type: string
    format: uuid
  product:
    $ref: ./ProductType.yaml
  dealer:
    $ref: ./Dealer.yaml
  creationDate:
    type: string
    format: date-time
  dueDate:
    type: string
    format: date-time
  nextActivityId:
    type: string
    format: uuid
  doneDate:
    type: string
    format: date-time
  status:
    $ref: ./ProspectStatus.yaml
  lostReason:
    $ref: ./ProspectLostReason.yaml
  savedSearchIds:
    type: array
    items:
      type: string
      format: uuid
  source:
    $ref: ./ProspectSource.yaml
  pipeId:
    type: string
    format: uuid
  pipeStepId:
    type: string
    format: uuid
  pipeStepPosition:
    type: integer
  positionInPipeStep:
    type: number
    format: double
  archiveDate:
    type: string
    format: date-time
  fees:
    type: number
    format: int64
  prospectionConsent:
    $ref: ./ProspectionConsent.yaml
  propertyPros:
    type: array
    items:
      type: string
  propertyCons:
    type: array
    items:
      type: string
