type: object
required:
  - crawlSource
  - url
properties:
  id:
    type: string
    format: uuid
  reference:
    type: boolean
  crawlSource:
    $ref: ./CrawlSource.yaml
  url:
    type: string
    format: uri
