type: object
properties:
  id:
    type: string
    format: uuid
  userId:
    type: integer
    format: int64
  version:
    type: string
    enum:
      - V1
      - V2
  agencyId:
    type: integer
    format: int64
  mandataryNetworkId:
    type: integer
    format: int64
  organizationId:
    type: integer
    format: int64
  externalIds:
    type: object
    properties:
      USER:
        type: string
      AGENCY:
        type: string
  name:
    type: string
  customer:
    $ref: ./Customer.yaml
  property:
    $ref: ./Property.yaml
  pages:
    type: array
    items:
      $ref: ./StudyPage.yaml
  sections:
    $ref: ./Sections.yaml
  creationDate:
    type: string
    format: date-time
  updateDate:
    type: string
    format: date-time
