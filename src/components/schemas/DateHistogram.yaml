type: object
properties:
  points:
    type: array
    items:
      $ref: ./DateHistogramPoint.yaml
    description: Liste de points
  type:
    type: string
    enum:
      - PERCENTILES
      - SUM
      - COUNT
      - AVERAGE
      - NONE
    example: PERCENTILES
    description: Type d'agrégation
