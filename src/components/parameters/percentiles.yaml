name: percentiles
in: query
description: Percentiles (uniquement `25.0`, `50.0` et `75.0` si `source=YANPORT`)
example:
  - '25.0'
  - '50.0'
  - '75.0'
style: form
schema:
  type: array
  items:
    type: number
    format: double
