name: marketingTypes
in: query
description: 'Types de marché (vente, location, vente en viager)'
style: form
schema:
  type: array
  items:
    type: string
    enum:
      - SALE
      - RENT
      - LIFE_ANNUITY
  example: SALE
