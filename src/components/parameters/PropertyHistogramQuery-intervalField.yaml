name: intervalField
in: query
description: Champ sur lequel sont calculées les statistiques
required: true
schema:
  type: string
  enum:
    - PRICE
    - PRICE_HC
    - PRICE_CC
    - PRICE_M2
    - PRICE_M2_HC
    - PRICE_M2_CC
    - SURFACE
    - ROOM_COUNT
    - DURATION_DAYS
    - MARKETING_DAYS
