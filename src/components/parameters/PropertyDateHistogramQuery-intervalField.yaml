name: intervalField
in: query
description: Champ de date utilisé pour les regroupements
required: true
schema:
  type: string
  enum:
    - PUBLICATION_DATE
    - DELETE_DATE
    - MARKETING_START_DATE
    - MARKETING_END_DATE
