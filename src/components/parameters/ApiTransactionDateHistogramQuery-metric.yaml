name: metric
in: query
required: true
description: méthode de calcul de l'agrégation
schema:
  type: array
  items:
    type: string
    enum:
      - percentiles
      - sum
      - count
      - average
